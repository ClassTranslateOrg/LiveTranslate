<!DOCTYPE html>
<html>
<head>
    <title>LiveTranslate Authentication</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            color: #4a90e2;
        }
        .container {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .btn {
            display: inline-block;
            background-color: #4a90e2;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 4px;
            margin-top: 20px;
        }
        .user-info {
            background-color: #e9ecef;
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
            overflow-x: auto;
        }
        .status-info {
            background-color: #d4edda;
            color: #155724;
            padding: 10px;
            border-radius: 4px;
            margin: 10px 0;
        }
        .error-info {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            border-radius: 4px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>LiveTranslate Authentication</h1>
        
        <div class="status-info">
            Server Status: Online
        </div>

        <% if (isAuthenticated) { %>
            <div>
                <h2>Welcome, <%= userInfo.name || userInfo.email || userInfo.username %></h2>
                <p>You are successfully authenticated.</p>
                
                <div class="user-info">
                    <pre><%= JSON.stringify(userInfo, null, 4) %></pre>
                </div>
                
                <a href="/auth/logout" class="btn">Logout</a>
                <a href="https://live-translate.org" class="btn">Go to LiveTranslate</a>
                <a href="http://localhost:3000" class="btn">Go to Local Development</a>
            </div>
        <% } else { %>
            <p>You need to authenticate to access LiveTranslate features.</p>
            <a href="/auth/login" class="btn">Login</a>
        <% } %>
        
        <div style="margin-top: 30px;">
            <h3>Troubleshooting</h3>
            <ul>
                <li>If you're experiencing connection issues, check that your DNS settings are properly configured.</li>
                <li>For local development, ensure both the frontend (port 3000) and backend (port 3001) servers are running.</li>
                <li>Make sure your browser allows WebRTC connections and has microphone/camera permissions.</li>
            </ul>
        </div>
    </div>
</body>
</html>
